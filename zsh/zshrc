# Homebrew
eval "$(/opt/homebrew/bin/brew shellenv)"

# zinit
ZINIT_HOME="${HOME}/.local/share/zinit/zinit.git"
source "${ZINIT_HOME}/zinit.zsh"

# zinit plugins
zinit light zsh-users/zsh-autosuggestions
zinit light zsh-users/zsh-syntax-highlighting

# OMZ snippets (lightweight, no full OMZ needed)
zinit snippet OMZP::git
zinit snippet OMZP::docker

# Starship prompt
eval "$(starship init zsh)"

# fnm (Node.js)
eval "$(fnm env --use-on-cd --shell zsh)"

# uv / Python (PATH only, no shell init needed)
export PATH="$HOME/.local/bin:$PATH"

# Aliases
alias vi='nvim'
alias vim='nvim'
alias ls='eza --icons --group-directories-first'
alias ll='eza --icons --group-directories-first -la'
alias dc='docker compose'
alias dm='docker-machine'
alias ipy='ipython'
alias tf='terraform'
alias gc='git checkout'
alias gw='git worktree'
alias cc="claude --dangerously-skip-permissions"
alias ccr="claude --dangerously-skip-permissions --resume"
alias pt='poetry'

# Functions
function gi() { curl -L -s https://www.gitignore.io/api/$@ ;}
function assh() { ssh -i ~/.ssh/howmuchhome.pem ubuntu@$@; }
eval "$(pyenv init -)"

# export LDFLAGS="-L/opt/homebrew/opt/postgresql@12/lib"
# export CPPFLAGS="-I/opt/homebrew/opt/postgresql@12/include"
# export PATH="/opt/homebrew/opt/postgresql@12/bin:$PATH"
# 
# export NODE_OPTIONS="--experimental-fetch"
# 
# # bun completions
# [ -s "/Users/yunseop/.bun/_bun" ] && source "/Users/yunseop/.bun/_bun"
# 
# # bun
# export BUN_INSTALL="$HOME/.bun"
# export PATH="$BUN_INSTALL/bin:$PATH"
# 
# # The following lines have been added by Docker Desktop to enable Docker CLI completions.
# fpath=(/Users/yunseop/.docker/completions $fpath)
# autoload -Uz compinit
# compinit
# # End of Docker CLI completions
#
export COLORTERM=truecolor
export BASH_MAX_OUTPUT_LENGTH=30000
export CLAUDE_CODE_FILE_READ_MAX_OUTPUT_TOKENS=25000
